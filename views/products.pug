extends layout

block content


  

  each val in data
    .card(style='width: 18rem;')
      .card-body
        h5.card-title= val.name
        p.card-text= val.manufacturer  
        a.btn.btn-primary(href='#') Show

